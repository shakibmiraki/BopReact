import { css } from "@emotion/core";

const styles = css({
  display: "flex",
  flex: 1,
  justifyContent: "center",
  alignItems: "center",
  height: "100%",
  "svg": {
    fontSize: "4rem",
  },
});

export default styles;
